name: dio_sse
description: Consume Server-Sent Events streams with Dio, including event filtering, error streams, and configurable reconnection.
version: 2.2.0
repository: https://github.com/liplum/dio_sse
topics:
  - dio
  - sse
  - server-sent-events
  - http

environment:
  sdk: ^3.9.0

dependencies:
  dio: ^5.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
